// ── Questions: authored (not in the guide's practice test) ───
// The official practice test covers 5 of the scenarios the study
// guide lists (it lists 8; the exam-guide PDF names 6).
// Scenario 6 (Structured Data Extraction) and scenario 4 (Developer
// Productivity Tools) appear on the real exam with no practice
// questions attached, so these are written here in the same format
// and difficulty band, grounded in the guide's theory chapters:
//   Ch.2 tools/tool_choice/JSON schemas   Ch.3 orchestration & hooks
//   Ch.6 prompt engineering   Ch.7 Message Batches
//   Ch.8 task decomposition   Ch.9 confidence calibration
//   Ch.11 context management   Ch.13 built-in tools
// dev-06..dev-10 deliberately weight Domain 1, the exam's biggest
// domain and the practice test's thinnest. Every answer traces to a
// stated rule; none invents behaviour.

export const AUTHORED = [
  // ── Structured Data Extraction ─────────────────────────────
  {
    id: 'ext-01',
    scenario: 'extraction',
    domain: 'd4',
    level: 'hard',
    situation: 'Your invoice extraction pipeline uses `tool_use` with a strict JSON schema. Output is always syntactically valid JSON with every required field present. But auditors find that 9% of extracted invoices have a `total` that does not equal the sum of their line items, and 4% carry a supplier VAT number in the `customer_vat` field.',
    ask: 'What does this tell you about the schema, and what should you do?',
    options: [
      { k: 'A', t: 'The schema is not strict enough, so add `minLength`, `pattern` and `format` constraints to every string field.' },
      { k: 'B', t: 'JSON schemas eliminate syntax errors but not semantic ones. Add validation of business rules after extraction and retry with the specific error in context.' },
      { k: 'C', t: 'Switch `tool_choice` from `auto` to `any` so the model is forced to produce structured output.' },
      { k: 'D', t: 'Mark `total` and `customer_vat` as optional so the model can omit them when unsure.' },
    ],
    answer: 'B',
    why: 'A schema guarantees shape, never meaning. Totals that do not reconcile and a value in the wrong field are both semantic errors, which is what post-extraction validation plus a retry carrying the concrete error ("total = 150 but sum(line_items) = 145") is for.',
    distractors: {
      A: 'String constraints catch malformed values, not correct-looking values that are wrong or misplaced.',
      C: 'You are already getting structured output every time. `tool_choice` changes whether a tool is called, not whether its arguments are right.',
      D: 'Making them optional hides the problem: the invoice does have a total, and the model would simply omit the field it gets wrong.',
    },
    pattern: 'Schemas fix syntax; validation fixes semantics',
    source: 'Authored · Ch.2.5, Ch.6.5',
  },
  {
    id: 'ext-02',
    scenario: 'extraction',
    domain: 'd4',
    level: 'hard',
    situation: 'You extract structured records from research papers. Around 20% of source documents genuinely do not state a funding source. Your schema marks `funding_source` as required, and review shows the model invents plausible-sounding funders for those documents rather than leaving them out.',
    ask: 'What is the correct schema change?',
    options: [
      { k: 'A', t: 'Add a post-extraction step that cross-checks each funder against a known-funders list and blanks anything unmatched.' },
      { k: 'B', t: 'Keep the field required and add a prompt instruction saying "never guess a funding source".' },
      { k: 'C', t: 'Make the field nullable (`"type": ["string", "null"]`) with a description saying null means the information was not found in the source.' },
      { k: 'D', t: 'Retry extraction on the documents that produce unmatched funders, including the previous incorrect answer.' },
    ],
    answer: 'C',
    why: 'A required field pushes the model to fabricate rather than admit a gap. A nullable type gives it a way to be honest, which is what you want when a fifth of your corpus legitimately has nothing to report.',
    distractors: {
      A: 'A whitelist cannot recognise a real funder that is not on it, and it treats the symptom rather than the schema forcing the fabrication.',
      B: 'The instruction conflicts with the schema, which still says the field must be present. The schema wins.',
      D: 'Retry cannot recover information that is absent from the source document. This is the classic case where retrying does not help.',
    },
    pattern: 'Required fields push the model to fabricate',
    source: 'Authored · Ch.2.4, Ch.6.5',
  },
  {
    id: 'ext-03',
    scenario: 'extraction',
    domain: 'd4',
    level: 'core',
    situation: 'Your support-ticket classifier uses an enum of `["bug", "feature", "docs"]`. Analysis shows 15% of tickets are forced into `bug` when they are really billing questions, access requests, or too vague to classify at all.',
    ask: 'How should you change the schema?',
    options: [
      { k: 'A', t: 'Expand the enum with every additional category you can enumerate from a sample of past tickets.' },
      { k: 'B', t: 'Add `"other"` and `"unclear"` to the enum plus a nullable detail string explaining the choice.' },
      { k: 'C', t: 'Remove the enum and let `category` be a free-text string.' },
      { k: 'D', t: 'Add a `confidence` number to the schema and route anything below 0.7 to a human.' },
    ],
    answer: 'B',
    why: 'Two different failures need two escape hatches. `"other"` plus a detail string catches real categories outside your list without losing the data; `"unclear"` lets the model say it cannot tell, which beats a confidently wrong label.',
    distractors: {
      A: 'A longer enum still has an edge, and it does nothing for tickets that are genuinely too vague to classify.',
      C: 'Free text destroys the consistency the enum was giving you and makes downstream routing unreliable.',
      D: 'Confidence scoring is a routing mechanism layered on top of a schema that still has nowhere to put a billing question.',
    },
    pattern: 'Give the schema a way to be honest',
    source: 'Authored · Ch.2.4',
  },
  {
    id: 'ext-04',
    scenario: 'extraction',
    domain: 'd4',
    level: 'hard',
    situation: 'Your validation-and-retry loop re-prompts Claude whenever extraction fails validation. It works well for date formats and arithmetic mismatches. But one class of failure retries three times and fails every time: the schema requires `contract_start_date`, and for those documents the date only appears in a separate amendment document that was never supplied to the model.',
    ask: 'What is the right response?',
    options: [
      { k: 'A', t: 'Increase the retry limit and add stronger wording about searching the whole document.' },
      { k: 'B', t: 'Recognise that retry cannot succeed when the information is external, and route these documents to a path that supplies the amendment or flags them for human handling.' },
      { k: 'C', t: 'Lower the temperature on retries so the model is more careful.' },
      { k: 'D', t: 'Add few-shot examples of contracts where the start date appears in unusual places.' },
    ],
    answer: 'B',
    why: 'Retry-with-feedback fixes format errors, misplaced fields and arithmetic the model can recheck. It cannot conjure a date that is not in the input. The fix is upstream: supply the missing document, or route the case to a human.',
    distractors: {
      A: 'More attempts at an impossible task produce more fabrications, not more answers.',
      C: 'Temperature does not create information the prompt never contained.',
      D: 'Examples teach where to look inside a document. The date is in a different document entirely.',
    },
    pattern: 'Know when retry cannot work',
    source: 'Authored · Ch.6.5',
  },
  {
    id: 'ext-05',
    scenario: 'extraction',
    domain: 'd4',
    level: 'core',
    situation: 'You must process 10,000 archived contracts by the end of the month, with no user waiting on any individual result. Cost is the main concern. You submit them as one batch of 10,000 and 340 fail because they exceed the context limit.',
    ask: 'What is the correct recovery?',
    options: [
      { k: 'A', t: 'Resubmit the entire batch of 10,000 with a chunking strategy applied to every document.' },
      { k: 'B', t: 'Switch the whole workload to synchronous calls so failures surface immediately.' },
      { k: 'C', t: 'Identify the 340 failures by `custom_id`, apply a chunking strategy to just those, and resubmit only them.' },
      { k: 'D', t: 'Split the batch into ten batches of 1,000 and resubmit all of them.' },
    ],
    answer: 'C',
    why: '`custom_id` exists to correlate each response to its source document, which is exactly what lets you isolate the 340 failures, change strategy for them alone, and avoid paying to re-process 9,660 documents that already succeeded.',
    distractors: {
      A: 'Reprocessing everything doubles the cost of the successful 9,660 for no benefit.',
      B: 'Synchronous costs twice as much for a workload with a month of slack and nobody waiting.',
      D: 'Smaller batches do not make an over-length document fit. The same 340 fail again.',
    },
    pattern: 'Correlate with custom_id, resubmit only failures',
    source: 'Authored · Ch.7.3, Ch.7.4',
  },
  {
    id: 'ext-06',
    scenario: 'extraction',
    domain: 'd4',
    level: 'hard',
    situation: 'A regulatory report must be delivered 30 hours from now, and it depends on a batch extraction job. The Message Batches API can take up to 24 hours with no latency SLA.',
    ask: 'How should you plan submission?',
    options: [
      { k: 'A', t: 'Submit within the next 6 hours, so that even a full 24-hour processing window lands before the deadline.' },
      { k: 'B', t: 'Submit at the 24-hour mark, leaving 6 hours of buffer for report assembly.' },
      { k: 'C', t: 'Submit now and poll every 15 minutes; if it has not completed in 12 hours, resubmit.' },
      { k: 'D', t: 'Use synchronous calls, since 30 hours is inside the batch window and the risk is not worth 50%.' },
    ],
    answer: 'A',
    why: 'Subtract the worst case from the deadline: 30 minus 24 leaves a 6-hour submission window. Anything submitted inside it still lands in time even if processing takes the full 24 hours.',
    distractors: {
      B: 'Submitting at hour 24 leaves 6 hours for a job that may take 24. It misses the deadline in the worst case.',
      C: 'Resubmitting a batch that is still legitimately processing duplicates the cost and does not shorten the window.',
      D: 'Thirty hours is not inside the window, it contains it. With correct planning this workload is a clean batch fit.',
    },
    pattern: 'Blocking is synchronous, scheduled is batch',
    source: 'Authored · Ch.7.5',
  },
  {
    id: 'ext-07',
    scenario: 'extraction',
    domain: 'd5',
    level: 'hard',
    situation: 'Your extraction system reports 97% overall accuracy on a labelled validation set, and you are preparing to remove human review entirely. A colleague objects.',
    ask: 'What should you do before automating?',
    options: [
      { k: 'A', t: 'Raise the sample size of the validation set until the confidence interval on 97% is narrow enough.' },
      { k: 'B', t: 'Break accuracy down by document type and by field, and run stratified random sampling over high-confidence extractions to check for hidden error pockets.' },
      { k: 'C', t: 'Automate now and monitor the aggregate accuracy metric for regressions.' },
      { k: 'D', t: 'Have the model self-rate confidence per document and automate only those above 90%.' },
    ],
    answer: 'B',
    why: 'An aggregate can hide a 40% error rate on one document type. Segmenting by type and field is what surfaces that, and stratified sampling of the high-confidence bucket is what catches errors the confidence score is not flagging.',
    distractors: {
      A: 'A tighter interval on the wrong number. The problem is that the average conceals the segments, not that it is imprecise.',
      C: 'The aggregate that is hiding the pocket now will keep hiding it in production.',
      D: 'Model self-rated confidence is poorly calibrated. Field-level scores tuned against labelled data are the guide’s answer, not a self-assessment threshold.',
    },
    pattern: 'Aggregate accuracy hides segment failure',
    source: 'Authored · Ch.9.4',
  },
  {
    id: 'ext-08',
    scenario: 'extraction',
    domain: 'd4',
    level: 'core',
    situation: 'Extracted records are syntactically valid but inconsistent in a way validation keeps catching: dates appear as "yesterday" and "03/04/2025", amounts as "five bucks" and "$5", proportions as "half" and "50%".',
    ask: 'What is the most effective addition?',
    options: [
      { k: 'A', t: 'Normalisation rules in the prompt: dates always ISO 8601 with relative dates resolved to absolute; currency as numeric amount plus currency code; percentages as decimal fractions.' },
      { k: 'B', t: 'A stricter JSON schema using `format: "date"` and a numeric type for amounts.' },
      { k: 'C', t: 'A post-processing library that parses each variant into a canonical form.' },
      { k: 'D', t: 'A `tool_choice` forcing a dedicated `normalize_values` tool after extraction.' },
    ],
    answer: 'A',
    why: 'The model is being asked for a value without being told what form it should take, so it copies the source. Explicit normalisation rules in the prompt define the target form, including the "yesterday" case that needs computing rather than reformatting.',
    distractors: {
      B: 'A `format` keyword rejects "yesterday" without telling the model what to produce instead, so you get a validation failure rather than a date.',
      C: 'A parser handles the variants you anticipated. "Five bucks" and "a pinch" are exactly the long tail that defeats rule-based normalisation.',
      D: 'A second tool call per record to reformat values the first call could have produced correctly.',
    },
    pattern: 'Explicit criteria beat vague adjectives',
    source: 'Authored · Ch.6.1',
  },
  {
    id: 'ext-09',
    scenario: 'extraction',
    domain: 'd4',
    level: 'hard',
    situation: 'You extract totals from receipts. Occasionally the printed total is itself wrong and does not match the items above it. Right now the pipeline silently returns whichever number the model favoured, and downstream reconciliation cannot tell a model error from a genuinely wrong receipt.',
    ask: 'How should you design the extraction?',
    options: [
      { k: 'A', t: 'Have the model return only the computed sum of line items, since it is derivable and therefore more reliable.' },
      { k: 'B', t: 'Have the model return the printed total, and reconcile against line items in a separate downstream service.' },
      { k: 'C', t: 'Have the model extract `stated_total` and `calculated_total` as separate fields with a `conflict_detected` boolean.' },
      { k: 'D', t: 'Route every receipt whose fields fail an arithmetic check to human review.' },
    ],
    answer: 'C',
    why: 'Extracting both values turns an invisible ambiguity into structured data. `conflict_detected` says a discrepancy exists without deciding what it means, so downstream can distinguish a misread from a genuinely incorrect receipt.',
    distractors: {
      A: 'Discarding the printed total loses the evidence that the receipt itself is wrong.',
      B: 'Reconciliation downstream needs both numbers, and this option only returns one of them.',
      D: 'Sensible as a follow-up, but the arithmetic check needs both values to exist first, so this is the consequence of C, not an alternative to it.',
    },
    pattern: 'Self-correction: extract stated and calculated',
    source: 'Authored · Ch.6.6',
  },
  {
    id: 'ext-10',
    scenario: 'extraction',
    domain: 'd4',
    level: 'hard',
    situation: 'You have four extraction tools, one per document family (invoice, contract, receipt, purchase order). Incoming documents are unlabelled. Occasionally the model answers a request with a paragraph of prose describing the document instead of calling any extraction tool at all, and the pipeline throws a parse error.',
    ask: 'Which `tool_choice` setting fixes this?',
    options: [
      { k: 'A', t: '`{"type": "auto"}`: the model still decides, and you handle the prose case with a retry.' },
      { k: 'B', t: '`{"type": "any"}`: the model must call one of the tools, and it still picks which one fits.' },
      { k: 'C', t: '`{"type": "tool", "name": "extract_invoice"}`: force a specific tool so output is always structured.' },
      { k: 'D', t: 'Merge the four tools into one `extract_document` with a union schema.' },
    ],
    answer: 'B',
    why: '`any` guarantees a tool call, and so structured output every time, while leaving the model free to choose which of the four schemas fits the document in front of it. That is exactly the shape of this problem.',
    distractors: {
      A: '`auto` is what is producing the prose responses today.',
      C: 'Forcing one specific tool guarantees structure and the wrong schema for three families out of four. Forced selection is for guaranteeing a first step, not for classification.',
      D: 'A union schema surrenders the per-family required fields and validation that make the four tools useful.',
    },
    pattern: 'tool_choice any guarantees structured output',
    source: 'Authored · Ch.2.3',
  },
  {
    id: 'ext-11',
    scenario: 'extraction',
    domain: 'd4',
    level: 'core',
    situation: 'You extract quantities from recipe text. Structured measurements ("250g flour") work fine. Informal ones fail constantly: "two handfuls of rice", "a pinch of salt", "a good glug of oil". Instructions telling the model to "estimate metric equivalents for informal measurements" produce wildly inconsistent numbers.',
    ask: 'What is the most effective change?',
    options: [
      { k: 'A', t: 'Add 3-4 few-shot examples showing informal phrases mapped to an amount, the original text, and a precision marker.' },
      { k: 'B', t: 'Build a lookup table of informal measurement terms and their metric equivalents.' },
      { k: 'C', t: 'Reject informal measurements at validation and flag those recipes for human entry.' },
      { k: 'D', t: 'Add a `precision` enum to the schema so the model marks these as approximate.' },
    ],
    answer: 'A',
    why: 'Informal units are too diverse for rules, which is why the instruction failed. Examples showing the full output shape (the estimate, the original phrase preserved, the precision marker) give the model a pattern it generalises to glugs and pinches it has never seen.',
    distractors: {
      B: 'A lookup table covers the terms you thought of. The tail is the entire problem here.',
      C: 'Rejecting them hands a large fraction of real recipes to humans.',
      D: 'A precision field labels the uncertainty without improving the estimate or preserving the source phrase.',
    },
    pattern: 'Few-shot beats more instructions',
    source: 'Authored · Ch.6.1',
  },

  // ── Developer Productivity Tools ───────────────────────────
  {
    id: 'dev-01',
    scenario: 'devtools',
    domain: 'd2',
    level: 'core',
    situation: 'An engineer new to a service asks the agent where a `PaymentDeclined` error originates. The agent immediately reads twelve files under `src/payments/` in full and exhausts most of the context window before locating anything.',
    ask: 'What is the correct investigation strategy?',
    options: [
      { k: 'A', t: 'Glob the payments directory first, then Read each match in dependency order.' },
      { k: 'B', t: 'Grep for the error string to find where it is raised, Read those files, Grep for their callers, Read those, building understanding incrementally.' },
      { k: 'C', t: 'Read every file once and rely on `/compact` when the window fills.' },
      { k: 'D', t: 'Ask the engineer to narrow the question to a specific file before starting.' },
    ],
    answer: 'B',
    why: 'Grep finds content, Read consumes it. Alternating the two (entry points, then the files that contain them, then their callers) expands only along the path that matters, instead of loading a directory to find one string.',
    distractors: {
      A: 'Glob finds files by name pattern; it cannot find which file raises a particular error. Reading all matches is the behaviour that caused the problem.',
      C: '`/compact` blurs the specifics the investigation exists to produce: file paths, line numbers, class names.',
      D: 'The engineer does not know which file; that is why they asked.',
    },
    pattern: 'Grep to locate, Read to understand',
    source: 'Authored · Ch.13.2',
  },
  {
    id: 'dev-02',
    scenario: 'devtools',
    domain: 'd2',
    level: 'core',
    situation: 'The agent tries to replace a `retries = 3` assignment in a config module using Edit. The call fails because that exact text appears in four places in the file.',
    ask: 'What is the documented fallback?',
    options: [
      { k: 'A', t: 'Read the full file, modify the content programmatically, and Write the updated version.' },
      { k: 'B', t: 'Use Bash with `sed` to target the line by number.' },
      { k: 'C', t: 'Call Edit repeatedly until the right occurrence is replaced.' },
      { k: 'D', t: 'Glob for similar files to work out which occurrence was intended.' },
    ],
    answer: 'A',
    why: 'When Edit cannot find a unique match, Read plus Write is the documented fallback: load the whole file, make the change with full context about which occurrence is which, and write the result back.',
    distractors: {
      B: 'Line-number editing through the shell is fragile and bypasses the file tools built for this.',
      C: 'Repeating a call that failed on ambiguity produces the same ambiguity.',
      D: 'Other files cannot disambiguate four occurrences inside this one.',
    },
    pattern: 'Read + Write when Edit cannot be unique',
    source: 'Authored · Ch.13.3, Domain 2.5',
  },
  {
    id: 'dev-03',
    scenario: 'devtools',
    domain: 'd2',
    level: 'hard',
    situation: 'You built an MCP server exposing `search_internal_docs`, which searches your company wiki, ADRs and runbooks, none of it in the repository. Logs show the agent almost always reaches for the built-in Grep instead, searching the codebase for information that only exists in the wiki, and reports it cannot find anything.',
    ask: 'What is the most effective fix?',
    options: [
      { k: 'A', t: 'Remove Grep from the agent’s `allowed_tools` so it must use the MCP tool.' },
      { k: 'B', t: 'Strengthen the MCP tool description to state the unique data it reaches (wiki pages, ADRs and runbooks that are not in the repository) and when to prefer it over a code search.' },
      { k: 'C', t: 'Add a system prompt instruction to always try `search_internal_docs` before Grep.' },
      { k: 'D', t: 'Rename the MCP tool to `grep_docs` so it reads as the natural counterpart to Grep.' },
    ],
    answer: 'B',
    why: 'Agents tend to prefer built-in tools over MCP tools with similar-sounding functions, and the counter is a description that spells out the concrete advantage, data that simply is not in the codebase, plus the boundary against the built-in alternative.',
    distractors: {
      A: 'Removing Grep breaks every legitimate code search to fix a documentation search.',
      C: '"Always try X first" is a blanket rule that misroutes genuine code searches, and it is probabilistic anyway.',
      D: 'A name that mimics Grep increases the overlap between the two tools rather than separating them.',
    },
    pattern: 'Tool descriptions are the selection mechanism',
    source: 'Authored · Ch.2.2',
  },
  {
    id: 'dev-04',
    scenario: 'devtools',
    domain: 'd5',
    level: 'hard',
    situation: 'Four hours into mapping an unfamiliar billing subsystem, the agent starts giving unstable answers: it refers to "the typical repository pattern" instead of the actual class names it read earlier, and contradicts a dependency it established two hours ago.',
    ask: 'What is the most effective practice for this phase of work?',
    options: [
      { k: 'A', t: 'Run `/compact` and continue in the same session.' },
      { k: 'B', t: 'Write the key findings (concrete classes, call sites, external limits, migration dates) to a scratchpad file the agent can consult later.' },
      { k: 'C', t: 'Start a new session and re-run the discovery from the beginning.' },
      { k: 'D', t: 'Use `--resume` on the session tomorrow, when the context has been cleared.' },
    ],
    answer: 'B',
    why: 'Those symptoms are context degradation, and a scratchpad survives it. Specific findings written to a file can be re-read after compaction or in a new session, so discovery does not have to be repeated to be trusted.',
    distractors: {
      A: '`/compact` is what turns "PaymentProcessor extends BaseProcessor" into "the typical repository pattern". It accelerates the failure.',
      C: 'Re-running four hours of discovery to recover findings you already had is the cost the scratchpad exists to avoid.',
      D: '`--resume` restores the degraded context along with tool results that may now be stale.',
    },
    pattern: 'Scratchpad findings survive context loss',
    source: 'Authored · Ch.11.4, Domain 5.4',
  },
  {
    id: 'dev-05',
    scenario: 'devtools',
    domain: 'd1',
    level: 'hard',
    situation: 'Three days ago you ran a long investigation in a named session, `investigation-auth-bug`. Since then two of the files involved were refactored and a dependency was upgraded. You want to continue the work.',
    ask: 'What should you do?',
    options: [
      { k: 'A', t: 'Run `claude --resume investigation-auth-bug` to pick up where you left off with full context.' },
      { k: 'B', t: 'Start a new session opening with a short summary of what the investigation established, and re-verify the parts that touched the changed files.' },
      { k: 'C', t: 'Use `fork_session` from the old session so the original stays intact.' },
      { k: 'D', t: 'Resume the session and instruct the agent to re-read every file before continuing.' },
    ],
    answer: 'B',
    why: 'The session’s tool results describe files that no longer exist in that form, and stale results actively mislead. A new session seeded with a short summary of the findings is more reliable than resuming a transcript full of outdated file contents.',
    distractors: {
      A: 'Resuming restores exactly the stale tool results that will make the agent reason about a codebase that has changed.',
      C: 'Forking is for exploring alternative approaches from shared context. It copies the stale context rather than fixing it.',
      D: 'Re-reading adds fresh content alongside the old, leaving contradictory versions of the same files in one context.',
    },
    pattern: 'Start fresh when tool results are stale',
    source: 'Authored · Ch.5.10, Domain 1.7',
  },
  {
    id: 'dev-06',
    scenario: 'devtools',
    domain: 'd1',
    level: 'core',
    situation: 'Your internal migration assistant has a `run_migration` tool for staging environments. Its system prompt says, in bold, "NEVER run a migration against production." In a month of staging drills it still targeted production twice when a connection string was ambiguous.',
    ask: 'What is the correct fix?',
    options: [
      { k: 'A', t: 'A `PreToolUse` hook that inspects every `run_migration` call and blocks any target that resolves to production.' },
      { k: 'B', t: 'Move the rule to the top of the system prompt and repeat it before every tool description.' },
      { k: 'C', t: 'Add few-shot examples of the agent refusing to migrate production.' },
      { k: 'D', t: 'Remove `run_migration` from `allowed_tools` entirely.' },
    ],
    answer: 'A',
    why: 'A production migration is exactly the class of rule that belongs in code, being destructive and irreversible, not in a prompt. The hook fires on 100% of calls and blocks before the tool runs; a prompt instruction is followed most of the time, and this stem has already measured the exceptions.',
    distractors: {
      B: 'Stronger wording raises compliance without ever reaching 100%. Two incidents in a month is what "most of the time" looks like.',
      C: 'Few-shot improves judgment on ambiguous cases; it does not make a guarantee out of a probability.',
      D: 'Removing the tool breaks the staging drills the assistant exists to run: capability lost, problem intact.',
    },
    pattern: 'Deterministic beats probabilistic',
    source: 'Authored · Ch.3.5, Domain 1.4-1.5',
  },
  {
    id: 'dev-07',
    scenario: 'devtools',
    domain: 'd1',
    level: 'core',
    situation: 'You define a repo-insights coordinator with `allowed_tools: ["Read", "Grep"]` and three specialist subagents for churn, ownership and test-coverage analysis. At runtime the coordinator never delegates. It grinds through every analysis itself, sequentially, and the run times out.',
    ask: 'What is the most likely cause?',
    options: [
      { k: 'A', t: 'The coordinator’s `allowed_tools` does not include `"Task"`, so it cannot spawn a subagent at all.' },
      { k: 'B', t: 'Subagents must be registered in `.mcp.json` before a coordinator can reach them.' },
      { k: 'C', t: 'Delegation requires `tool_choice: "any"` on the coordinator.' },
      { k: 'D', t: 'The coordinator’s `max_tokens` is too low to plan a delegation.' },
    ],
    answer: 'A',
    why: 'Task is the tool that spawns subagents, and an agent can only call tools on its whitelist. Without `"Task"` in `allowed_tools`, delegation is silently impossible, and the coordinator does the only thing it can: everything, itself.',
    distractors: {
      B: '`.mcp.json` wires MCP servers, not subagents. Subagents are reached through the Task tool.',
      C: '`tool_choice: "any"` forces some tool call; it does not grant access to a tool outside the whitelist.',
      D: 'A low cap truncates output; it does not remove a capability.',
    },
    pattern: 'A coordinator without Task cannot delegate',
    source: 'Authored · Ch.3.3, Domain 1.2-1.3',
  },
  {
    id: 'dev-08',
    scenario: 'devtools',
    domain: 'd1',
    level: 'hard',
    situation: 'Your weekly engineering digest always aggregates the same four sources in the same order: merged PRs, incident reports, deploy logs, on-call notes. A teammate proposes rebuilding it around a coordinator that decides each week how to decompose the work.',
    ask: 'How should you evaluate the proposal?',
    options: [
      { k: 'A', t: 'Keep a fixed pipeline: the steps are known, stable and identical every run, so deterministic stages beat run-time decomposition.' },
      { k: 'B', t: 'Adopt dynamic decomposition; a coordinator is always the more capable architecture.' },
      { k: 'C', t: 'Merge everything into one agent with a larger context window.' },
      { k: 'D', t: 'Spawn one subagent per repository instead of per source.' },
    ],
    answer: 'A',
    why: 'Dynamic decomposition earns its overhead when the shape of the work varies per request. Here the sources, the order and the output are fixed, so a pipeline of deterministic stages is simpler, cheaper and easier to debug, with an LLM call only where judgment is needed.',
    distractors: {
      B: 'A coordinator deciding what to do each week adds cost and nondeterminism to a task whose decomposition never changes.',
      C: 'One agent carrying four sources in one window is the coverage-thins-as-it-fills failure the split exists to avoid.',
      D: 'Partitioning by repository cuts across all four sources, recreating the aggregation problem inside every subagent.',
    },
    pattern: 'Fixed steps get a pipeline, open topics get a coordinator',
    source: 'Authored · Ch.8.1-8.2, Domain 1.6',
  },
  {
    id: 'dev-09',
    scenario: 'devtools',
    domain: 'd1',
    level: 'core',
    situation: 'After a long shared investigation into a flaky test suite, two competing fixes emerge: mock the external service, or stand up a dedicated test database. You want to develop both far enough to compare, without either analysis leaking into the other.',
    ask: 'What is the right session mechanic?',
    options: [
      { k: 'A', t: 'Use `fork_session` twice at the end of the investigation, developing one approach per fork.' },
      { k: 'B', t: 'Try the mock approach first in the same session, then the database approach after it.' },
      { k: 'C', t: 'Start two fresh sessions and paste the ticket description into each.' },
      { k: 'D', t: 'Resume the investigation session twice in parallel with `--resume`.' },
    ],
    answer: 'A',
    why: 'Forking branches independent sessions from the shared context up to the branch point: both forks inherit the investigation, then diverge with no cross-contamination. That is exactly the compare-two-approaches case fork_session exists for.',
    distractors: {
      B: 'Sequential attempts in one session contaminate each other: the second approach is reasoned about through the residue of the first.',
      C: 'Fresh sessions throw away the shared investigation both approaches depend on, so each fork re-derives it, differently.',
      D: 'Resuming the same named session twice does not create independent branches; it re-enters one history.',
    },
    pattern: 'fork_session compares approaches from shared context',
    source: 'Authored · Ch.5.10, Domain 1.7',
  },
  {
    id: 'dev-10',
    scenario: 'devtools',
    domain: 'd1',
    level: 'hard',
    situation: 'Your changelog agent delegates to three summarizers (features, fixes, breaking changes) whose inputs are fully independent. Logs show the coordinator issues one `Task` call, waits for the result, then issues the next: total latency is the sum of all three.',
    ask: 'What change gets the parallelism back?',
    options: [
      { k: 'A', t: 'Have the coordinator emit all three `Task` calls in a single turn, since multiple Task calls in one turn run concurrently.' },
      { k: 'B', t: 'Raise the coordinator’s `max_tokens` so it can plan all three delegations at once.' },
      { k: 'C', t: 'Insert a fourth "dispatcher" subagent whose job is to spawn the other three.' },
      { k: 'D', t: 'Switch the summarizers to a faster model so the sequential chain completes sooner.' },
    ],
    answer: 'A',
    why: 'Parallel fan-out is a property of how the Task calls are issued: several Task calls in one coordinator turn run in parallel; one call per turn serializes them. The prompt should tell the coordinator to dispatch all independent slices together.',
    distractors: {
      B: 'The cap limits response length. It has no effect on whether tool calls in separate turns serialize.',
      C: 'A dispatcher just moves the same one-call-per-turn behaviour down a level and adds a hop.',
      D: 'A faster model shrinks each stage but keeps the sum-of-stages shape; the structural fix is concurrency.',
    },
    pattern: 'Independent slices fan out in one turn',
    source: 'Authored · Ch.3.3, Domain 1.2',
  },
];
