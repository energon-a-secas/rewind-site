// ── Questions: Code Generation with Claude Code ──────────────
// Exam scenario 2. CLAUDE.md, rules, skills, slash commands,
// planning mode, MCP config, session and context strategy.
// Source: official guide practice test Q31-Q45.

export const CODEGEN = [
  {
    id: 'cg-01',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'You asked Claude Code to write a function transforming API responses into an internal normalised format. Two iterations in, the structure is still wrong — some fields nested differently, timestamps formatted incorrectly. You described the requirements in prose and Claude interprets them differently each time.',
    ask: 'Which approach is most effective for the next iteration?',
    options: [
      { k: 'A', t: 'Write a JSON schema for the expected output and validate Claude’s result against it after each iteration.' },
      { k: 'B', t: 'Provide 2-3 concrete input-output examples showing the expected transformation for representative API responses.' },
      { k: 'C', t: 'Rewrite the requirements with more technical precision — exact field mappings, nesting rules, timestamp format strings.' },
      { k: 'D', t: 'Ask Claude to explain its current understanding of the requirements to find where interpretations diverge.' },
    ],
    answer: 'B',
    why: 'Prose about nesting is exactly the thing that admits multiple readings, which is why two iterations diverged. A worked pair shows the nesting and the timestamp format with no room to interpret.',
    distractors: {
      A: 'A schema catches a wrong result after the fact; it does not communicate the transformation you want.',
      C: 'More precise prose is the approach that has already failed twice.',
      D: 'Diagnosing the divergence costs a turn and still leaves you describing the target in words.',
    },
    pattern: 'Show the target, do not describe it',
    source: 'Guide Q31',
  },
  {
    id: 'cg-02',
    scenario: 'codegen',
    domain: 'd3',
    level: 'hard',
    situation: 'You need to add Slack as a notification channel. The codebase has clear patterns for email, SMS and push. But Slack offers fundamentally different integrations: incoming webhooks (simple, one-way), bot tokens (delivery confirmation and programmatic control), or Slack Apps (two-way events, needs workspace approval). The task says "add Slack support" with no integration method specified and no stated need for delivery tracking.',
    ask: 'How should you approach this?',
    options: [
      { k: 'A', t: 'Direct execution with incoming webhooks, matching the existing one-way notification pattern.' },
      { k: 'B', t: 'Switch to planning mode to explore the integration options and their architectural implications, then present a recommendation before implementing.' },
      { k: 'C', t: 'Direct execution: scaffold a Slack channel class on the existing patterns and defer the integration decision.' },
      { k: 'D', t: 'Direct execution with a bot token so delivery confirmation stays possible.' },
    ],
    answer: 'B',
    why: 'Two plan triggers fire at once: multiple viable approaches with materially different architecture, and ambiguous requirements. Planning mode explores the trade-offs safely and gets agreement before code exists to throw away.',
    distractors: {
      A: 'Picking webhooks by analogy assumes one-way is acceptable, which is precisely what nobody has decided.',
      C: 'A scaffold that defers the decision bakes in structural assumptions the decision may invalidate.',
      D: 'Choosing bot tokens for a capability nobody asked for buys workspace-approval complexity on a guess.',
    },
    pattern: 'Plan when the approach is genuinely undecided',
    source: 'Guide Q32',
  },
  {
    id: 'cg-03',
    scenario: 'codegen',
    domain: 'd3',
    level: 'hard',
    situation: 'Your `CLAUDE.md` has grown past 400 lines: coding standards, testing conventions, a detailed PR review checklist, deployment instructions, database migration procedures. You want coding standards and testing conventions always applied, but PR review, deploy and migration guidance to apply only when doing those tasks.',
    ask: 'Which restructuring is most effective?',
    options: [
      { k: 'A', t: 'Move everything into Skills organised by workflow type, leaving only a brief project description in `CLAUDE.md`.' },
      { k: 'B', t: 'Keep everything in `CLAUDE.md` but use `@import` syntax to split it into separately maintained files by category.' },
      { k: 'C', t: 'Split `CLAUDE.md` into `.claude/rules/` files with path-bound glob patterns so each rule loads for the relevant file types.' },
      { k: 'D', t: 'Keep universal standards in `CLAUDE.md` and create Skills for the workflow-specific guidance (PR review, deploy, migrations) with trigger keywords.' },
    ],
    answer: 'D',
    why: 'The split in the stem is always-on versus task-triggered, and that maps directly onto the two surfaces. `CLAUDE.md` loads every session, so standards live there; Skills are invoked on demand, which is exactly what "only when doing those tasks" means.',
    distractors: {
      A: 'Moving the coding standards into Skills means they are absent unless someone remembers to invoke them.',
      B: '`@path` imports keep the content modular but every import still loads every session. Nothing becomes conditional.',
      C: 'Path globs trigger on which file you edit. Deployment and PR review are activities, not file types.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q33',
  },
  {
    id: 'cg-04',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'You are restructuring a monolithic application into microservices. It touches dozens of files and requires decisions about service boundaries and module dependencies.',
    ask: 'Which approach should you choose?',
    options: [
      { k: 'A', t: 'Switch to planning mode to explore the codebase, understand dependencies and design the approach before making changes.' },
      { k: 'B', t: 'Start in direct execution and switch to planning only after hitting unexpected complexity.' },
      { k: 'C', t: 'Start in direct execution with incremental changes, letting implementation reveal natural service boundaries.' },
      { k: 'D', t: 'Use direct execution with detailed upfront instructions specifying each service structure.' },
    ],
    answer: 'A',
    why: 'Every plan trigger is present: dozens of files, several defensible boundary decisions, and architectural consequences. Planning mode explores with no side effects and produces a plan you approve before anything is rewritten.',
    distractors: {
      B: 'Reactive switching means you discover the complexity by having already made expensive changes.',
      C: 'Boundaries that "emerge" from incremental edits are the ones you are stuck with, not the ones you chose.',
      D: 'Specifying each service upfront assumes you already know the dependency structure. That is the thing you need to find out.',
    },
    pattern: 'Plan when the approach is genuinely undecided',
    source: 'Guide Q34',
  },
  {
    id: 'cg-05',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'Your team created an `/analyze-codebase` skill that does deep analysis — dependency scanning, coverage counts, quality metrics. After running it, developers report Claude becomes less responsive and loses track of the original task.',
    ask: 'How do you fix this while keeping full analysis capability?',
    options: [
      { k: 'A', t: 'Add `context: fork` to the skill frontmatter so the analysis runs in an isolated subagent context.' },
      { k: 'B', t: 'Add `model: haiku` to use a faster, cheaper model for the analysis.' },
      { k: 'C', t: 'Split the skill into three smaller skills, each producing less output.' },
      { k: 'D', t: 'Instruct the skill to compress all results into a short summary before displaying them.' },
    ],
    answer: 'A',
    why: 'The verbose output is flooding the main session, which is what `context: fork` exists for. The analysis runs at full depth in an isolated subagent and only its result crosses back, so the main session keeps the original task.',
    distractors: {
      B: 'A cheaper model produces the same volume of output into the same context window.',
      C: 'Three skills producing a third each still dump the whole analysis into the main session.',
      D: 'Summarising after generation means the verbose output has already been produced in the main context.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q35',
  },
  {
    id: 'cg-06',
    scenario: 'codegen',
    domain: 'd3',
    level: 'hard',
    situation: 'Your team uses a `/commit` skill at `.claude/skills/commit/SKILL.md`. A developer wants to customise it for their own workflow — different commit message format, extra checks — without affecting teammates.',
    ask: 'What do you recommend?',
    options: [
      { k: 'A', t: 'Create a personal version under `~/.claude/skills/` with a different name, e.g. `/my-commit`.' },
      { k: 'B', t: 'Add conditional logic based on username in the project skill frontmatter.' },
      { k: 'C', t: 'Create a personal version at `~/.claude/skills/commit/SKILL.md` with the same name.' },
      { k: 'D', t: 'Set `override: true` in the personal skill frontmatter to prioritise it over the project version.' },
    ],
    answer: 'A',
    why: 'A different name avoids the collision entirely: the developer keeps the team’s maintained `/commit` — and every update the team ships to it — plus a clearly named personal skill. No teammate is affected and nothing is silently shadowed.',
    distractors: {
      B: 'Username branching pushes one person’s preferences into the shared, version-controlled file.',
      C: 'Works mechanically — personal skills take precedence over project skills with the same name — but that is the trap: the shadowing is silent, so this developer quietly stops receiving team updates to `/commit` and has to remember they run a different skill under the same command.',
      D: 'No such frontmatter key. Precedence is positional, not declared.',
    },
    pattern: 'Personal variants get their own name',
    source: 'Guide Q36',
  },
  {
    id: 'cg-07',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'After months of use, three developers report Claude reliably follows the guidance "always include comprehensive error handling", while a fourth who just joined says Claude ignores it. All four work in the same repo with up-to-date code.',
    ask: 'What is the most likely cause and fix?',
    options: [
      { k: 'A', t: 'The guidance lives in the original developers’ user-level `~/.claude/CLAUDE.md`, not the project `.claude/CLAUDE.md`. Move it to the project-level file.' },
      { k: 'B', t: 'The new developer’s `~/.claude/CLAUDE.md` has conflicting instructions overriding project settings; they should delete that section.' },
      { k: 'C', t: 'Claude Code learns per-user preferences over time; the new developer must repeat the requirement until it sticks.' },
      { k: 'D', t: 'Claude Code caches `CLAUDE.md` after first read and the original developers are on cached versions. Everyone should clear the cache.' },
    ],
    answer: 'A',
    why: 'Same repo, same code, different behaviour per person — the instruction is not in the repo. User-level config is not version controlled, so it reached three laptops and never reached the fourth. Project level is where shared standards belong.',
    distractors: {
      B: 'Possible in principle, but it does not explain why three separate developers all get behaviour the repo does not specify.',
      C: 'Claude Code does not learn preferences across sessions this way. Persistence comes from config files.',
      D: 'Invented mechanism, and caching would not differ by developer on identical files.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q37',
  },
  {
    id: 'cg-08',
    scenario: 'codegen',
    domain: 'd3',
    level: 'hard',
    situation: 'You find that including 2-3 full endpoint implementations as context markedly improves consistency when generating new API endpoints. That context is only useful when creating endpoints — not when debugging, reviewing, or doing other work in the API directory.',
    ask: 'Which configuration approach is most effective?',
    options: [
      { k: 'A', t: 'Add the endpoint examples and pattern documentation to the project `CLAUDE.md` so they are always available.' },
      { k: 'B', t: 'Reference the endpoint examples manually in every generation request by pasting code into the prompt.' },
      { k: 'C', t: 'Configure path-specific rules in `.claude/rules/api/` that include the examples and activate when working in the API directory.' },
      { k: 'D', t: 'Create a skill that references the examples and the pattern instructions, invoked on demand via a slash command.' },
    ],
    answer: 'D',
    why: 'The trigger is the task, not the file. A skill loads the examples when you are generating an endpoint and stays out of the way when you are debugging one — which is the distinction the stem draws explicitly.',
    distractors: {
      A: 'Always-loaded means those examples sit in context during every debugging and review session too.',
      B: 'Manual pasting works but is unrepeatable and inconsistent across the team.',
      C: 'Path rules fire on any edit in the API directory, including the debugging and review work you want them absent from.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q38',
  },
  {
    id: 'cg-09',
    scenario: 'codegen',
    domain: 'd3',
    level: 'hard',
    situation: 'Your `/migration` skill generates database migration files and takes the name via `$ARGUMENTS`. Three problems in production: developers run it with no arguments and get badly named files; it sometimes reuses schema details from unrelated earlier conversations; and one developer triggered a destructive test cleanup because the skill had broad tool access.',
    ask: 'Which configuration fixes all three?',
    options: [
      { k: 'A', t: 'Use positional `$1`/`$2` instead of `$ARGUMENTS`, reference schema files via `@` syntax, and add a frontmatter description warning about destructive operations.' },
      { k: 'B', t: 'Add `argument-hint` to request the required parameter, use `context: fork` to isolate execution, and restrict `allowed-tools` to file-write operations.' },
      { k: 'C', t: 'Split into `/migration-create` and `/migration-apply`, add validation instructions requesting the name if missing, and use different `allowed-tools` per skill.' },
      { k: 'D', t: 'Add validation instructions to `SKILL.md` for `$ARGUMENTS`, add prompts telling Claude to ignore prior conversation context, and list prohibited operations.' },
    ],
    answer: 'B',
    why: 'Three symptoms, three frontmatter keys. `argument-hint` prompts for the missing name, `context: fork` isolates the run so unrelated conversation cannot leak in, and `allowed-tools` makes the destructive command unavailable rather than discouraged.',
    distractors: {
      A: 'Positional parameters do not prompt when omitted, and a "warning" in a description is not enforcement.',
      C: 'Splitting the skill addresses none of the three causes; the naming and context-leak problems survive in both halves.',
      D: 'All three fixes are prompt instructions — probabilistic where two of the three problems demand a guarantee.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q39',
  },
  {
    id: 'cg-10',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'Your codebase mixes conventions: React components use functional style with hooks, API handlers use async/await with specific error handling, database models follow the repository pattern. Test files sit next to the code they test (`Button.test.tsx` beside `Button.tsx`), and you want all tests to follow the same conventions wherever they live.',
    ask: 'What is the most supported way to apply the right conventions automatically?',
    options: [
      { k: 'A', t: 'Put every convention in the root `CLAUDE.md` under headings per area and let Claude infer which applies.' },
      { k: 'B', t: 'Create skills in `.claude/skills/` for each code type, embedding the conventions in each `SKILL.md`.' },
      { k: 'C', t: 'Place a separate `CLAUDE.md` in each subdirectory with that area’s conventions.' },
      { k: 'D', t: 'Create rule files under `.claude/rules/` with YAML frontmatter glob patterns that apply conventions by file path.' },
    ],
    answer: 'D',
    why: 'The trigger is which file is being edited, which is what `paths:` globs match. A pattern like `**/*.test.tsx` catches every co-located test regardless of directory, and the rule only loads when it is relevant.',
    distractors: {
      A: 'One flat file relies on the model inferring which section applies, which is the thing you are trying to make deterministic.',
      B: 'Skills are on-demand. Conventions should apply whenever the file is touched, not when someone remembers a command.',
      C: 'Directory-level files break exactly on the case described: tests scattered beside their source across the whole tree.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q40',
  },
  {
    id: 'cg-11',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'You want a custom `/review` slash command that runs your team’s standard code review checklist, available to every developer who clones or updates the repository.',
    ask: 'Where should you create the command file?',
    options: [
      { k: 'A', t: 'In `~/.claude/commands/` in each developer’s home directory.' },
      { k: 'B', t: 'In the project repository under `.claude/commands/`.' },
      { k: 'C', t: 'In `.claude/config.json` as an array of commands.' },
      { k: 'D', t: 'In the root project `CLAUDE.md`.' },
    ],
    answer: 'B',
    why: 'Project commands live in the repo, so they are version controlled and arrive automatically with a clone or pull. That is precisely the "available to everyone" requirement.',
    distractors: {
      A: 'User-level commands are personal and not shared, so every developer would have to install it themselves.',
      C: 'No such file. Commands are markdown files in a directory, not entries in a config array.',
      D: '`CLAUDE.md` holds instructions, not command definitions.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q41',
  },
  {
    id: 'cg-12',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'Your team’s `CLAUDE.md` is past 500 lines, mixing TypeScript conventions, testing guidance, API patterns and deployment procedures. Developers struggle to find and update the right section.',
    ask: 'What does Claude Code support for organising project instructions into focused topical modules?',
    options: [
      { k: 'A', t: 'A `.claude/config.yaml` mapping file patterns to specific sections inside `CLAUDE.md`.' },
      { k: 'B', t: 'Separate Markdown files in `.claude/rules/`, each covering one topic (`testing.md`, `api-conventions.md`).' },
      { k: 'C', t: 'Instructions split into `README.md` files in relevant subdirectories, which Claude loads automatically.' },
      { k: 'D', t: 'Multiple `CLAUDE.md` files at different levels of the directory tree, each overriding its parent.' },
    ],
    answer: 'B',
    why: '`.claude/rules/` is the supported alternative to a monolithic `CLAUDE.md`: one Markdown file per topic, each independently maintainable, and each able to carry `paths:` frontmatter for conditional loading.',
    distractors: {
      A: 'No such file. Section-level mapping inside `CLAUDE.md` is not a Claude Code feature.',
      C: '`README.md` files are documentation for humans; Claude Code does not load them as instructions.',
      D: 'Directory-level files exist, but they scope by location rather than by topic, and they do not "override" parents.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q42',
  },
  {
    id: 'cg-13',
    scenario: 'codegen',
    domain: 'd3',
    level: 'core',
    situation: 'You created an `/explore-alternatives` skill your team uses to brainstorm and evaluate implementation approaches before choosing one. Developers report that afterwards Claude’s responses are coloured by the discussion — referencing rejected approaches, retaining exploration context that interferes with the actual implementation.',
    ask: 'How should you configure this skill?',
    options: [
      { k: 'A', t: 'Use the `!` prefix in the skill to run the exploration logic as a bash subprocess.' },
      { k: 'B', t: 'Add `context: fork` to the skill frontmatter.' },
      { k: 'C', t: 'Split into `/explore-start` and `/explore-end` to mark where exploration context should be discarded.' },
      { k: 'D', t: 'Create the skill in `~/.claude/skills/` instead of `.claude/skills/`.' },
    ],
    answer: 'B',
    why: '`context: fork` runs the skill in an isolated subagent, so the brainstorm — including every rejected approach — never enters the main conversation history and cannot bias the implementation that follows.',
    distractors: {
      A: 'A bash subprocess runs shell commands. It has nothing to do with conversation context.',
      C: 'Marker commands cannot remove text that is already in the transcript.',
      D: 'Where a skill is stored controls who can run it, not how its output pollutes context.',
    },
    pattern: 'Match the config surface to the trigger',
    source: 'Guide Q43',
  },
  {
    id: 'cg-14',
    scenario: 'codegen',
    domain: 'd2',
    level: 'core',
    situation: 'Your team wants a GitHub MCP server for searching PRs and checking CI status from Claude Code. Six developers each have their own personal GitHub access token. You want consistent tooling across the team without committing credentials.',
    ask: 'Which configuration approach is most effective?',
    options: [
      { k: 'A', t: 'Have each developer add the server in user scope via `claude mcp add --scope user`.' },
      { k: 'B', t: 'Build an MCP wrapper that reads tokens from a `.env` file and proxies GitHub API calls, then add the wrapper to the project `.mcp.json`.' },
      { k: 'C', t: 'Add the server to the project `.mcp.json` using environment variable substitution (`${GITHUB_TOKEN}`) and document the required variable in the README.' },
      { k: 'D', t: 'Configure the server in project scope with a placeholder token and tell developers to override it locally.' },
    ],
    answer: 'C',
    why: 'This is what `.mcp.json` env substitution is for: one version-controlled definition of the server that everyone shares, with each developer supplying their own token through the environment. No secret is ever committed.',
    distractors: {
      A: 'Six independent user-scope configs drift. There is no single source of truth for the team.',
      B: 'A custom proxy is a service to build and maintain for something the config format already does.',
      D: 'A committed placeholder plus local overrides recreates the drift problem and invites someone to commit a real token.',
    },
    pattern: 'Project config in VCS, secrets by environment',
    source: 'Guide Q44',
  },
  {
    id: 'cg-15',
    scenario: 'codegen',
    domain: 'd5',
    level: 'hard',
    situation: 'You are adding error-handling wrappers around external API calls across a 120-file codebase, in three phases: discover all call sites and patterns, collaboratively design the approach, then implement consistently. In phase 1 Claude produces enormous output listing hundreds of call sites with context, filling the context window before discovery even finishes.',
    ask: 'Which approach completes the task while keeping implementation consistent?',
    options: [
      { k: 'A', t: 'Use an Explore subagent for phase 1 to isolate the verbose discovery output and return a summary, then run phases 2-3 in the main conversation.' },
      { k: 'B', t: 'Do everything in the main conversation, using `/compact` periodically to reduce context as you move through files.' },
      { k: 'C', t: 'Switch to headless mode with `--continue`, passing explicit context summaries between batch calls.' },
      { k: 'D', t: 'Define the error-handling pattern in `CLAUDE.md`, then process files in batches across multiple sessions.' },
    ],
    answer: 'A',
    why: 'Discovery is verbose and disposable; design and implementation need retained context. An Explore subagent absorbs the hundreds of call sites in its own window and returns a summary, leaving the main context free for the two phases that actually benefit from it.',
    distractors: {
      B: '`/compact` blurs specifics, and the call-site list is exactly the specifics phase 3 depends on.',
      C: 'Headless batching removes the collaborative design phase the task explicitly requires.',
      D: '`CLAUDE.md` carries the pattern but not the discovered call sites, so consistency across sessions is unenforced.',
    },
    pattern: 'Delegate discovery to protect the main context',
    source: 'Guide Q45',
  },
];
